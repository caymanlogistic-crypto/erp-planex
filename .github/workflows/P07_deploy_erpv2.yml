name: P07 Legacy Deploy ERPV2 (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy deploy disabled
        shell: bash
        run: |
          echo 'Legacy P07 GitHub deploy trigger is disabled by P21.'
          echo 'Use the ERPv2 Controlled Deploy workflow with an explicit source_sha and confirmation.'
          exit 1
