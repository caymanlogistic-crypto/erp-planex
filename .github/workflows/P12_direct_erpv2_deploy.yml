name: P12 Legacy Direct ERPV2 Deploy (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Legacy P12 direct deployment workflow disabled by P21.'
          echo 'Use ERPv2 Controlled Deploy.'
          exit 1
