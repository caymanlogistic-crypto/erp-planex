name: P15 Legacy Cleanup Restore (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'Legacy P15 cleanup/restore workflow disabled by P21 because its runtime behavior is not guaranteed read-only.'
          exit 1
