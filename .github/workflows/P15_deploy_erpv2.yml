name: P15 Legacy Deploy ERPV2 (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'Legacy P15 deployment workflow disabled by P21.'
          echo 'Use ERPv2 Controlled Deploy.'
          exit 1
