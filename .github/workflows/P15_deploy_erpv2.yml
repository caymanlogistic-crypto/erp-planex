name: P15 Legacy Deploy ERPV2 (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Legacy deploy disabled
        shell: bash
        run: |
          echo 'Legacy P15 deploy is disabled by P21.'
          echo 'Use ERPv2 Controlled Deploy with an explicit source_sha and DEPLOY_ERPV2 confirmation.'
          exit 1
