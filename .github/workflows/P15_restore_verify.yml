name: P15 Legacy Restore Verify (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'Legacy P15 restore/verify workflow disabled by P21 because it can mutate ERP data through UI.'
          exit 1
