name: P16 Legacy Deploy ERPV2 (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'Legacy P16 deploy workflow disabled by P21.'
          echo 'Use ERPv2 Controlled Deploy.'
          exit 1
