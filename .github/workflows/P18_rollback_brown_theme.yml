name: P18 Legacy Rollback ERPV2 (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy rollback disabled
        shell: bash
        run: |
          echo 'Legacy P18 GitHub rollback trigger is disabled by P21.'
          echo 'Use ERPv2 Controlled Deploy and provide the exact accepted rollback source_sha explicitly.'
          exit 1
