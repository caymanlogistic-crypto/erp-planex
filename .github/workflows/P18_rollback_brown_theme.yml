name: P18 Legacy Rollback ERPV2 (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Legacy rollback disabled
        shell: bash
        run: |
          echo 'Legacy P18 rollback is disabled by P21.'
          echo 'Use ERPv2 Controlled Deploy with the exact rollback source_sha.'
          exit 1
