name: P19 Legacy PATCH_A Exact Deploy V2 (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'Legacy P19 exact deployment V2 workflow disabled by P21.'
          echo 'Use ERPv2 Controlled Deploy.'
          exit 1
