name: P22 Legacy Client Fix Deploy (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Legacy deploy disabled
        shell: bash
        run: |
          echo 'P22 automatic deploy disabled by P21 control-plane stabilization.'
          echo 'Use ERPv2 Controlled Deploy with an explicit source_sha and DEPLOY_ERPV2 confirmation.'
          exit 1
